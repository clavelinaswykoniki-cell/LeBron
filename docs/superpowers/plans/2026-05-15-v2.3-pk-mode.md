# v2.3 段位对抗（前端 mock 优先）

> 创建：2026-05-15
> 等待：用户自己做域名备案 + ECS 后端搭建（7-15 天）
> 现在做：前端 UI + 本地逻辑，mock 数据先跑通

---

## 产品形态：A + B 组合

- **A 排行榜**：全网段位榜（mock 100 个假玩家 + 你自己），显示"我的排名 #X"
- **B PK 答题**：5 题答题对战，每题 4 选项（1 正确反驳 / 3 黑粉话术），得分影响段位
- **C 对战记录**（顺手）：本地存历史 PK 战绩

## 文件冲突避免

新建独立目录，不动现有文件：
- `miniprogram/pages/leaderboard/` 4 文件（Agent F16）
- `miniprogram/pages/pk/` 4 文件（Agent F17）
- `miniprogram/utils/duel.js` 主 Claude 写（PK 算分 + mock 玩家生成器）

主 Claude 接入：
- `app.json` 注册 2 新页面
- `index.js` menuEntries 加 "段位排行" + "PK 对战"
- `index.wxml` 不动（菜单自动渲染）

## 数据契约（mock 与未来 API 通用）

```js
// duel.js 暴露的接口（后端接通后改实现，调用方不变）
duel.getLeaderboard() → [{rank, name, avatar, viewCount, copyCount, score}, ...]
duel.getMyRank() → {rank, total, score, nickname}
duel.startMatch() → {matchId, questions: [{cardId, options: [...]}]}
duel.submitMatch(matchId, answers) → {score, rankChange, history}
duel.getDuelHistory() → [{matchId, score, date, opponent}, ...]
```

## 后端切换（备案完后）

`duel.js` 内部：
- `USE_MOCK = true`（v2.3）→ 用 mock 数据 + localStorage
- `USE_MOCK = false`（备案完）→ 走 wx.request 调 ECS API

切换 1 行代码。

## 验收

1. 9 个测试全过（无回归）
2. 排行榜页面打开能看到 mock 100 玩家 + 我的高亮
3. PK 页能答 5 题，结束显示得分 + 段位变化
4. 战绩存 localStorage，再次进入可见历史
