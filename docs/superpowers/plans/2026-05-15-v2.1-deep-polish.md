# v2.1 工程深化 + 内容扩展 + 分发就绪 Plan

> **创建**：2026-05-15 03:00
> **来源**：用户要求继续完善，预算 ~40w token
> **执行**：10 个 subagent 并行（独立文件范围），主 Claude 接入共享文件
> **总目标**：工程基础 + 内容翻倍 + 分发就绪

---

## 10 个并行 Agent 分工

### Tier A: 工程基础

**Agent F1** — GitHub Actions CI
- 新建 `.github/workflows/ci.yml`
- node 18+，跑 5 个 npm test
- PR / push main 触发
- README 加 CI badge

**Agent F2** — OSS 仪式三件套
- 新建 `LICENSE`（MIT 文本）
- 新建 `CONTRIBUTING.md`（开发流程 / 提交规范 / 测试要求）
- 新建 `CHANGELOG.md`（v1.0 / v2.0 / v2.1 三段历史）

**Agent F3** — 3 个新单元测试
- `scripts/test-matchquery.js`：核心匹配的 6+ 边界 case
- `scripts/test-safety.js`：safety wrappers stub wx
- `scripts/test-feedback.js`：tactileFeedback stub wx
- 写入 `package.json` 三条 script

### Tier B: 内容深化

**Agent F4** — 扩展卡 JSON
- 新建 `miniprogram/data/rebuttal_cards_extended.js`
- 10 张已有素材卡（黑点 A-J）→ 每张生成 events/data/causes/background/analysis 5 字段
- 数据来自 `docs/raw-perspectives/black_*.md`
- 导出 `extendedById: { [card_id]: { events, data, causes, background, analysis } }`

**Agent F5** — 5 张新黑点卡入 corpus
- 新建 `miniprogram/data/rebuttal_cards_v2_1.js`
- 5 张卡（泡泡冠军 / 21 后 LAL 崩盘 / 没招牌动作 / G7 表现 / 季后赛胜率）
- 新建 `miniprogram/data/aliases_v2_1.js`：每张 ~6 别名
- **不改** rebuttal_cards.js / aliases.js / arsenal.js（主 Claude 集成）

**Agent F6** — 20 张球星对比深化卡
- 新建 `miniprogram/data/rebuttal_cards_stars.js`
- 20 张：Curry / Durant / Jokic / Giannis / Wembanyama / Tatum 各 3-4 张深度对比
- 新建 `miniprogram/data/aliases_stars.js`
- **不改** rebuttal_cards_extra.js / aliases_extra.js / arsenal.js

### Tier C: UX 提升

**Agent F7** — 搜索历史 + 收藏页
- 新建 `miniprogram/utils/storage.js`
  - `recordSearchHistory(query)` / `getSearchHistory()` / `clearHistory()`
  - `toggleFavorite(cardId)` / `getFavorites()` / `isFavorited(cardId)`
  - localStorage keys: `lbr_search_history`, `lbr_favorites`
- 新建 `miniprogram/pages/history/` （4 文件）
- 新建 `miniprogram/pages/favorites/`（4 文件）
- **不改** index.js / app.json

**Agent F8** — 分享卡片生成
- 新建 `miniprogram/utils/cardShare.js`
  - `generateShareImage(cardData)` 用 wx.canvas API 生成图，return tempFilePath
  - 卡片设计：紫金背景 + 黑粉话术 + 反驳 + 数据点 + watermark "詹黑逻辑拆解器 v2.1"
- 写一个 simple smoke (不实际跑 canvas，但 require 验证 syntax)
- **不改** index.js

**Agent F9** — 首页 onboarding
- 新建 `miniprogram/pages/onboarding/`（4 文件）
- 4 步引导：欢迎 → 搜黑点 → 复制爽感 → 段位
- 用 localStorage key `lbr_onboarded` 防重复展示
- **不改** index.js / app.json

### Tier D: 分发就绪

**Agent F10** — 分发包
- 新建 `LANDING_PAGE.html`（单页静态营销页，紫金主题，截图占位）
- 新建 `docs/PRESS_KIT.md`（项目介绍 + logo concept + 数据 + 截图清单）
- 新建 `docs/ROADMAP.md`（v3.0 候选方向：辩论训练 / 短视频博主弹药库 / 双视角观点聚合器）

---

## 主 Claude 收尾任务

1. **数据接入** — 改 `miniprogram/data/arsenal.js`
   - 加载 rebuttal_cards_v2_1 + rebuttal_cards_stars 进卡片列表
   - 加载 aliases_v2_1 + aliases_stars 进别名表
   - 暴露 extendedById（来自 F4）让 index 可查

2. **app.json 注册** — 4 个新页面（history / favorites / onboarding + 之前已注册的）

3. **index.js 接入**
   - import storage（search history + favorites）
   - onGenerate 时 recordSearchHistory
   - onLoad 检查 lbr_onboarded → 第一次跳 onboarding
   - 点收藏按钮 toggleFavorite
   - 长按结果卡 → 分享卡片
   - 加入口 4 个：history / favorites / share / about（已有）

4. **index.wxml 接入**
   - 卡片右上加星标（收藏）
   - menu-grid 加 history / favorites 入口
   - 加 long-press 触发分享

5. **README 更新** — v2.1 章节 + CI badge

6. **跑测试** — 8 个测试全过（旧 5 + 新 3）

7. **devils-advocate critique** — 修 FAIL

8. **commit + push GitHub**

---

## 文件冲突避免（关键）

每个 agent **只新建** utils 或 page 或 docs 文件。**禁止改**：
- `miniprogram/pages/index/*`
- `miniprogram/app.json`
- `miniprogram/data/arsenal.js`
- `miniprogram/data/rebuttal_cards.js` / `rebuttal_cards_extra.js`
- `miniprogram/data/aliases.js` / `aliases_extra.js`
- `README.md`
- `package.json`（除 F3 加 test scripts）

主 Claude 在 Step 4 统一改这些。

---

## 验收终态

- ✅ 8 个测试通过（原 5 + matchquery + safety + feedback）
- ✅ 10 个新 utils / pages / docs 文件就位
- ✅ 新增 25+ 张反驳卡，总数 150 → 175+
- ✅ devils-advocate critique 18/20 PASS 以上
- ✅ GitHub Actions CI 触发通过
- ✅ push 到 GitHub
